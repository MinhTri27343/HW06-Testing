# HW06 - Kiểm thử API

## 1. Thông tin chung

| Mục | Nội dung |
|---|---|
| Mã bài tập | HW06-AI |
| Thời lượng | 10 giờ |
| Hạn nộp | Vui lòng xem liên kết nộp bài trên Moodle |
| Hình thức | Bài tập cá nhân |
| Nộp bài | Moodle (báo cáo) |
| Giảng viên và trợ giảng | TS. Lâm Quang Vũ / TS. Trần Duy Hoàng / ThS. Trần Thị Bích Hạnh / ThS. Trương Phước Lộc / ThS. Hồ Tuấn Thanh |
| Liên hệ | lqvu@fit.hcmus.edu.vn / tdhoang@fit.hcmus.edu.vn / ttbhanh@fit.hcmus.edu.vn / tploc@fit.hcmus.edu.vn / hthanh@fit.hcmus.edu.vn |
| Chính sách AI | Mở - bắt buộc có bản khai báo và đính kèm Báo cáo Kiểm toán AI |
| Cấp độ Bloom-AI yêu cầu | G9.1 đến G9.6, tùy theo bài tập (xem phần Ánh xạ CLO) |

## 2. Nguyên tắc định hướng

Các nguyên tắc sau quy định cách bạn được yêu cầu làm việc trong toàn bộ chuỗi bài tập của học phần. Hãy đọc kỹ trước khi bắt đầu vì bài nộp sẽ được đánh giá dựa trên các nguyên tắc này.

- **Chiến lược AI-First.** Bạn bắt buộc phải áp dụng AI vào các kỹ thuật kiểm thử đã học trên lớp. Tuy nhiên, điều này không có nghĩa là chỉ đưa ra một câu lệnh chung chung, chẳng hạn như: "hãy tạo tất cả ca kiểm thử API từ đặc tả và chạy chúng". Thay vào đó, bạn phải hướng dẫn AI qua từng bước của kỹ thuật theo đúng nội dung đã được giảng dạy, sử dụng AI như một trợ lý có kỷ luật chứ không phải một hộp đen.

- **Rà soát bởi con người.** Mọi kết quả do AI tạo ra đều phải được chính bạn, với tư cách sinh viên, rà soát cẩn thận. Bạn hoàn toàn chịu trách nhiệm về tính chính xác của các kết quả này. Bạn phải thực hiện mọi chỉnh sửa và cải tiến cần thiết; không được nộp nguyên trạng đầu ra của AI khi chưa rà soát.

- **Báo cáo Kiểm toán AI.** Toàn bộ quá trình sử dụng AI phải được ghi lại trong một nhật ký đầy đủ. Bạn được khuyến khích xây dựng các Agent Skill có thể tự động thực hiện những hoạt động này cho các bài tập tương tự. Nếu không sử dụng AI, bạn vẫn phải khai báo rõ điều đó.

- **Tài liệu hóa.** Toàn bộ quá trình làm việc phải được ghi lại ở định dạng văn bản, chẳng hạn như Markdown.

- **Chất lượng quan trọng hơn việc hoàn thành.** Bài làm không chỉ được chấm dựa trên việc đã hoàn thành hay chưa, mà còn dựa trên số lượng và chất lượng của các sản phẩm bàn giao: ca kiểm thử, bản kiểm toán AI, Postman collection và báo cáo Newman, báo cáo lỗi, thiết kế bộ sinh kiểm thử và các liên kết tham chiếu.

## 3. Chuẩn đầu ra

Sau khi hoàn thành bài tập này, bạn sẽ có khả năng:

- Sử dụng AI để tạo các ca kiểm thử API từ đặc tả API của SUT, sau đó kiểm toán và mở rộng chúng.
- Thiết kế kiểm thử API bao phủ các phân hoạch miền, chuyển đổi trạng thái, bảo mật và xác thực lược đồ.
- Phát hiện các lỗi mà AI bỏ sót, đặc biệt là các lỗi liên quan đến bảo mật và chuyển đổi trạng thái.
- Thiết kế một bộ sinh kiểm thử dùng AI cho SUT.
- Thể hiện năng lực Bloom-AI ở các cấp độ G9.2 (Áp dụng), G9.3 (Phân tích), G9.4 (Cộng tác) và G9.5 (Sáng tạo).

## 4. Hệ thống cần kiểm thử (SUT)

**SUT:** EShop - một ứng dụng thương mại điện tử mẫu bằng tiếng Việt, được thiết kế để thực hành kiểm thử.

**Kho mã nguồn:** <https://github.com/ttbhanh/eshop-sut>

Các chức năng của ứng dụng được tổ chức thành các nhóm sau:

### Nhóm A - Xác thực, Danh mục và Sản phẩm

- FR-01: Đăng ký tài khoản
- FR-02: Đăng nhập và khóa tài khoản
- FR-03: Quên mật khẩu và đặt lại mật khẩu (hai bước)
- FR-04: Quản lý hồ sơ cá nhân
- FR-05: Danh sách và tìm kiếm sản phẩm
- FR-06: Xem chi tiết sản phẩm

### Nhóm B - Giỏ hàng và Thanh toán

- FR-07: Giỏ hàng
- FR-08: Thanh toán
- FR-09: Phiếu giảm giá
- FR-10: Máy trạng thái đơn hàng
- FR-11: Xem lịch sử đơn hàng (người dùng)

### Nhóm C - Trang quản trị web

- FR-12: Kiểm soát truy cập
- FR-13: Bảng điều khiển
- FR-14: Quản lý danh mục (CRUD)
- FR-15: Quản lý sản phẩm (CRUD)
- FR-16: Nhập sản phẩm từ CSV
- FR-17: Quản lý phiếu giảm giá (CRUD)
- FR-18: Quản lý đơn hàng (quản trị viên)
- FR-19: Quản lý người dùng (quản trị viên)

### Nhóm D - Ứng dụng di động

SUT đi kèm một đặc tả API trong kho mã nguồn (`api_specification.md`); hãy tham khảo tệp này để biết các endpoint hiện có. Đặc tả cũng định nghĩa các yêu cầu bảo mật SEC-01 đến SEC-07.

## 5. Lựa chọn API

Chọn ba (3) API, trong đó mỗi API triển khai một chức năng thuộc một trong ba Nhóm A, B và C. Nhóm D (ứng dụng di động) không được sử dụng ở đây vì HW06 tập trung vào API backend. Hãy tham khảo đặc tả API để tìm các endpoint của từng chức năng đã chọn.

- **Nhóm A:** ví dụ, đăng nhập (FR-02) hoặc danh sách/tìm kiếm sản phẩm (FR-05).
- **Nhóm B:** ví dụ, giỏ hàng (FR-07) hoặc thanh toán/tạo đơn hàng (FR-08, FR-10).
- **Nhóm C:** ví dụ, một thao tác quản trị sản phẩm hoặc đơn hàng có thay đổi trạng thái (FR-15, FR-18).

Tương tự các bài tập trước, hãy bảo đảm lựa chọn của bạn không trùng với các thành viên khác trong nhóm: không có hai thành viên nào được chọn cùng một bộ ba API.

## 6. Yêu cầu

Với mỗi API trong ba API đã chọn, hãy hoàn thành quy trình sau. Ghi lại quá trình trong báo cáo chính và đính kèm các bằng chứng bắt buộc. Hãy xem lại các bài giảng liên quan đến kiểm thử API trước khi bắt đầu.

1. **Tạo bằng AI.** Cung cấp đặc tả API của SUT cho một công cụ AI và hướng dẫn công cụ đó từng bước, không dùng một câu lệnh chung chung duy nhất, để tạo các ca kiểm thử cho API (mục tiêu: ít nhất 35 ca cho mỗi API). Các ca kiểm thử phải bao phủ:
   - phân hoạch miền cho mọi tham số (ví dụ: định dạng email, độ phức tạp của mật khẩu, giá lớn hơn 0);
   - chuyển đổi trạng thái (FR-10: `pending` -> `confirmed` -> `shipping` -> `delivered`, cùng các quy tắc hủy đơn);
   - bảo mật (SEC-01 đến SEC-07, ví dụ: SQL injection, IDOR, leo thang đặc quyền);
   - xác thực lược đồ (cấu trúc phản hồi phải khớp chính xác với đặc tả).

2. **Kiểm toán (rà soát bởi con người).** Gắn nhãn từng ca kiểm thử do AI tạo là `VALID`, `INVALID` hoặc `INCOMPLETE`, kèm lý do; đồng thời sửa các ca không hợp lệ hoặc chưa đầy đủ. Bạn hoàn toàn chịu trách nhiệm về các ca kiểm thử cuối cùng.

3. **Mở rộng.** Tự bổ sung ít nhất năm ca kiểm thử mà AI đã bỏ sót, đặc biệt là các ca liên quan đến bảo mật và chuyển đổi trạng thái; giải thích vì sao AI bỏ sót chúng (do chất lượng câu lệnh, giới hạn của mô hình hoặc đặc điểm của API).

4. **Thực thi.** Chạy các ca kiểm thử bằng Postman + Newman (hoặc Karate/RestAssured). Mọi request phải có header `X-Student-Id: {StudentID}` (ví dụ, thông qua pre-request script). Xuất báo cáo Newman/HTML.

5. **Báo cáo lỗi.** Báo cáo mọi lỗi thực tế tìm được, bao gồm cả các lỗi AI đã bỏ sót, trong báo cáo Markdown và trên trang GitHub Issues của bạn; mỗi issue phải đính kèm một ảnh chụp màn hình.

Ngoài ra, các yêu cầu kỹ thuật sau áp dụng cho toàn bộ bộ kiểm thử:

- Tận dụng hợp lý càng nhiều tính năng của Postman càng tốt, ví dụ: workspace, collection, variable, environment, chạy theo dữ liệu (Collection Runner với tệp dữ liệu), monitor và mock server. Liệt kê trong báo cáo các tính năng Postman bạn đã sử dụng. Người dùng Karate/RestAssured cần trình bày các tính năng công cụ tương đương.

- **Tích hợp vào CI/CD.** Thêm các ca kiểm thử API vào một pipeline CI/CD cho SUT (ví dụ, chạy Newman bằng GitHub Actions trong kho mã nguồn của bạn), đồng thời viết một báo cáo CI/CD ngắn mô tả cấu hình pipeline và hai lần chạy bên dưới, kèm ảnh chụp màn hình và liên kết. Cung cấp hai commit mẫu: một commit có lần chạy pipeline cho thấy tất cả ca kiểm thử đều đạt, và một commit có lần chạy pipeline cho thấy một ca kiểm thử thất bại.

## 7. Agent Skill

Để đạt cấp độ Sáng tạo (G9.5), hãy thiết kế một bộ sinh kiểm thử API dùng AI cho SUT: khi nhận đặc tả API, hệ thống sẽ tự động tạo các ca kiểm thử.

Cung cấp sơ đồ do chính bạn vẽ và mã giả của thiết kế. ("Do chính bạn vẽ" có nghĩa là bạn tự đưa ra các quyết định thiết kế; có thể sử dụng bất kỳ công cụ vẽ sơ đồ nào, nhưng bản thân sơ đồ không được tạo bởi AI.)

Bạn được khuyến khích triển khai thiết kế dưới dạng một Agent Skill có thể tái sử dụng và nộp video minh họa (liên kết YouTube) cho thấy công cụ tạo kiểm thử cho một API.

## 8. Công cụ được phép và cấp độ Bloom-AI

Bạn có thể sử dụng các công cụ sau và phải khai báo chúng trong Báo cáo Kiểm toán AI:

- Bất kỳ công cụ AI nào bạn chọn (ví dụ: ChatGPT, Claude, Gemini, Copilot, Cursor).
- Postman + Newman (mặc định) hoặc Karate/RestAssured (thay thế).
- Không bắt buộc: các công cụ kiểm thử LLM (Promptfoo, DeepEval, Ragas).

Các cấp độ Bloom-AI bắt buộc cho bài tập này là G9.2 (Áp dụng), G9.3 (Phân tích), G9.4 (Cộng tác) và G9.5 (Sáng tạo).

## 9. Báo cáo Kiểm toán AI (phụ lục bắt buộc)

Đính kèm Báo cáo Kiểm toán AI dưới dạng phụ lục. Có thể sử dụng nội dung của các Mẫu AI được cung cấp nếu cần.

Nếu không sử dụng AI, hãy khai báo:

> Tôi không sử dụng bất kỳ sự hỗ trợ nào của AI trong bài tập này.

Nếu có sử dụng AI, hãy khai báo:

> Tôi sử dụng các công cụ AI cho những nhiệm vụ sau.

Với mỗi lần tương tác, hãy cung cấp các thông tin sau:

- Tên công cụ AI
- Ngày và giờ
- Câu lệnh của bạn
- Đầu ra của AI

Để đơn giản hóa quá trình này, bạn được khuyến khích tạo một skill hoặc rule có khả năng tự động trích xuất các thông tin trên sau mỗi phiên làm việc với AI.

## 10. Phê bình AI (bắt buộc, 200-300 từ)

Viết một đoạn văn dài 200-300 từ phê bình AI. Trả lời các câu hỏi sau: AI đã sai, thiên lệch hoặc chưa đầy đủ ở điểm nào? Vì sao AI không phát hiện được vấn đề? Bạn đã rút ra nguyên tắc gì về việc cộng tác với AI trong bài tập này?

Có thể sử dụng nội dung của các Mẫu AI được cung cấp nếu cần.

## 11. Các ràng buộc chống gian lận bằng AI

Bài tập này dựa trên bằng chứng thực thi có thật và có thể truy xuất nguồn gốc. Các nội dung sau không được tạo hoặc làm giả bằng AI; trợ giảng sẽ xác minh chúng khi chấm bài:

- Header `X-Student-Id: {StudentID}`, được chứng minh bằng ảnh chụp màn hình console từ pre-request script của bạn.
- Đầu ra của lần chạy Newman, trong đó hostname phải khớp với môi trường triển khai của bạn (`localhost` hoặc `127.0.0.1` được chấp nhận).
- Sơ đồ bộ sinh kiểm thử AI phải do chính bạn vẽ, tức là do bạn thiết kế chứ không được AI tạo trực tiếp.

## 12. Nhật ký commit Git

Tạo một commit Git mới cho mỗi bước của quy trình (ví dụ: tạo, kiểm toán, mở rộng và thực thi đối với từng API).

Cung cấp nhật ký commit Git ở định dạng tệp văn bản.

## 13. Vấn đáp

30% sinh viên được chọn ngẫu nhiên có thể được mời tham gia buổi vấn đáp kéo dài 5-7 phút trong tuần sau hạn nộp để giải thích cách mình hoàn thành bài tập.

## 14. Quy định nộp bài

**Định dạng tên tệp:** `<StudentID>_HW06_AI_API_<SelfAssessedGrade>.zip`

`SelfAssessedGrade` là một số có 3 chữ số trong khoảng `[000, 100]`.

**Ví dụ:** `25127001_HW06_AI_API_090.zip`

Nội dung bắt buộc của tệp `.zip`:

- Báo cáo chính (Markdown + PDF), bao gồm báo cáo kiểm thử API và bản kiểm toán AI.
- Liên kết đến kho GitHub công khai (collection, script và báo cáo).
- Postman collection (`.json`) và báo cáo Newman (HTML), cùng danh sách các tính năng Postman đã sử dụng.
- Một báo cáo CI/CD ngắn: cấu hình pipeline và hai lần chạy pipeline mẫu (một lần tất cả đều đạt, một lần có ca kiểm thử thất bại), kèm ảnh chụp màn hình và liên kết.
- Các ca kiểm thử và bản tóm tắt kiểm thử ở định dạng Excel.
- Sơ đồ và mã giả của bộ sinh kiểm thử AI (`PNG`/Mermaid + `.md`/`.py`).
- Không bắt buộc: đặc tả API được chuyển đổi sang OpenAPI (`.yaml`/`.json`); nếu được tạo bằng AI thì cũng phải kiểm toán.
- Báo cáo lỗi, kèm ảnh chụp màn hình các lỗi trên trang GitHub Issues.
- Phần Phê bình AI và Báo cáo Kiểm toán AI (Markdown + PDF).
- Nhật ký commit Git (tệp văn bản).
- Tệp `README.md` chứa bảng tự đánh giá (bên dưới) và báo cáo tóm tắt kiểm thử: số lượng API; số ca kiểm thử được tạo, bổ sung, thực thi, đạt và thất bại; cùng số lượng lỗi.
- Mọi tài liệu hỗ trợ khác.

Nộp bài trên Moodle. Xem hạn nộp tại liên kết nộp bài.

## 15. Mẫu đánh giá

| STT | Tiêu chí | Điểm | Điểm tự đánh giá |
|---:|---|---:|---:|
| 1 | API 1 - toàn bộ quy trình (tạo + kiểm toán + mở rộng + thực thi + lỗi) | 30 | |
| 2 | API 2 - toàn bộ quy trình (cùng tiêu chí) | 30 | |
| 3 | API 3 - toàn bộ quy trình (cùng tiêu chí) | 30 | |
| 4 | Agent Skill (bộ sinh kiểm thử dùng AI) | 10 | |
| | **Tổng cộng** | **100** | |

## 16. Tài liệu tham khảo

- ISTQB Foundation Level Syllabus (phiên bản mới nhất).
- Hardman, P. (2025). *A Post-AI Learning Taxonomy*.
- Fuster Rabella, M. (2025). *OECD Education Working Paper No. 338*.
- Anthropic (2025). *Building Reliable AI Test Agents* - blog kỹ thuật.
- Tài liệu DeepEval và Promptfoo - các framework kiểm thử LLM.

## 17. Các quy định khác

- Không chấp nhận nộp bài trễ.
- Thiếu bất kỳ tài liệu bắt buộc nào sẽ bị 0 điểm.
- Sao chép giữa các sinh viên, bao gồm cả câu lệnh, sẽ khiến cả hai bên bị 0 điểm.

